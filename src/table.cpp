#include "../header/table.hpp"
//#include "../gc_3d_defs.hpp"

//using namespace GC_3D;

void Table::CreateTableModelMesh(Model* Mesh1, Model* Mesh2, int size)
{
	/*for (int i = 0; i < size; i++)
	{
		if (!ChangeMesh)
		{
			ModelMesh.push_back(&Mesh1);
			ChangeMesh = true;
		}
		else if (ChangeMesh)
		{
			ModelMesh.push_back(&Mesh2);
			ChangeMesh = false;
		}
	}*/
}

void Table::SetTableModelMesh(Model* Mesh1, Model* Mesh2)
{
}
